--insecure

# Default write-out format (each line separately)
# write-out "\n\nDownload Statistics:\n\ Downloaded bytes: %{size_download}\n\ Header size: %{size_header}\n\ Request size: %{size_request}\n\ Content-Type: %{content_type}\n\ Response code: %{response_code}\n\ Total time: %{time_total}s\n" \
